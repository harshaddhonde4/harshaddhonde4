name: Generate Snake

on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:

jobs:
  generate:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Generate Snake Animation
        uses: Platane/snk@v3
        with:
          github_user_name: harshaddhonde4
          outputs: |
            dist/github-contribution-grid-snake.svg
            dist/github-contribution-grid-snake-dark.svg?palette=github-dark
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Push to output branch
        uses: crazy-max/ghaction-github-pages@v3.1.0
        with:
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
```

Click **Commit changes**

---

### Step 3 — Manually Run It
1. Click **Actions** tab
2. Click **Generate Snake** in the left sidebar
3. Click **Run workflow** dropdown → **Run workflow**
4. Wait ~60 seconds for green ✅

---

### Step 4 — Verify it worked
After green ✅, visit:
```
https://github.com/harshaddhonde4/harshaddhonde4/tree/output
